
// Exibe o ano atual
console.log(2020);

// Comentário de uma linha

/* Comentário 
   de múltiplas
   linhas
*/

// Tipo de dados
let numerico = 0; // número
let string = "string"; // string
let boolean = true; // booleano
let nulo = null; // nulo
let indefinido = undefined; // indefinido
let simbolo = "i don't know yet";
let objeto = {string, boolean };

// Operadores aritméticos
console.log(1 + 2); // adição
console.log(3 - 4); // subtração
console.log(5 * 6); // multiplicação
console.log(7 / 8); // divisão
console.log(9 % 10); // resto da divisão

// Concatenação de string
console.log('Um' + ', ' + 'dois');

// Propriedades
console.log("Christopher".length) // Exemplo de uma propriedade

// Métodos
// Classes podem ter métodos, métodos são ações, é algo especifico que uma classe faz, podem ter um retorno ou não
console.log('Gerando número aleatório de 0 a 100: ', Math.random * 100);
console.log('Tudo isso ficará em maiúsculo: '.toUpperCase());

// Objetos built-in
/* São objetos imbutidos do JavaScript, um de exemplo é objeto interno Math, para operações matemáticas
   mais infos aqui: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math
*/
console.log(Math.abs(2))
